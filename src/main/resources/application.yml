server:
  port: 8080

spring:
  application:
    name: haeyo-back
  config:
    import: optional:file:.env[.properties]
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/haeyo_db
    username: postgres
    password: q1w2e3
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

jwt:
  secret: ${JWT_SECRET:your-secret-key-here-please-change-this-to-a-long-secure-random-string-at-least-256-bits}
  expire-time: ${JWT_EXPIRE_TIME:86400000}
  refresh-expire-time: ${JWT_REFRESH_EXPIRE_TIME:1209600000}

cookie:
  refresh-token:
    name: refreshToken
    http-only: true
    secure: ${COOKIE_REFRESH_SECURE:false}
    same-site: Strict
    path: /api/auth

vworld:
  api:
    key: C52BE737-A08C-392F-978D-57BF695EEC19

kakao:
  api:
    key: 2b98c5dcee1dfc0fa7a3ff5a779bcec4

naver:
  client-id: YOUR_NAVER_CLIENT_ID
  client-secret: YOUR_NAVER_CLIENT_SECRET

logging:
  level:
    com.hy.haeyoback: DEBUG
    org.springframework.web: INFO
