spring:
  application:
    name: haeyo-back

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

jwt:
  secret: ${JWT_SECRET}
  expire-time: ${JWT_EXPIRE_TIME:3600000}
